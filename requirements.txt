# ShopMate-R requirements
# Install inside a Python 3.10+ venv:
#   pip install -r requirements.txt

openai>=1.0
flask>=3.0
requests>=2.28
SpeechRecognition>=3.10
PyAudio>=0.2.14
paramiko>=3.0   # for real Pepper SSH control
